# Roadmap

The 14 product phases from the brief, grouped by delivery horizon. The **first real
milestone is the vertical slice** — a deliberately thin path through Phases 1–7 — proven on
seeded data before any single phase is built to full breadth.

## Signature vertical slice (the north-star demo)

```
Organization → Supplier → Supplier uploads a sustainability document →
TRACE ingests & processes → AI extracts emissions/activity data (spans + confidence) →
Human reviews & approves → Evidence created & linked → Calculation executed (versioned) →
Trust Score generated → Scope 3 updated → Compliance (ESRS) mapping updated →
Audit readiness updated
```

Delivering this end to end is worth more than 50 disconnected features.

## Near term — prove the spine

| Phase | Scope | Definition of done |
| --- | --- | --- |
| **0 — Foundation** ✅ | `/docs`, ADR-001…005, repo scaffold plan. | A new engineer understands the system from `docs/` before opening code. |
| **1 — App foundation** 🚧 *(landed; live-DB verification pending)* | Monorepo (pnpm + Turborepo), `@trace/{config,shared,domain,db}`, NestJS API (magic-link auth, permission RBAC guard, CSRF guard, hash-chained audit interceptor path, OpenAPI), Next.js shell + navigation + auth/onboarding/members/roles/audit-log screens, BullMQ worker skeleton, Prisma schema + migrations (init + RLS + audit-guard), tenant-scoped `withOrgContext`, NordWerk seed, CI. | A user creates an org, invites users, logs in, works in a secure tenant-isolated workspace; every consequential action is audit-logged. **Status:** builds, typechecks, lints, unit tests green; RLS + migrations + seed run in CI against Postgres; not yet run against a live DB on the dev machine (no local Postgres). |
| **2 — Supplier intelligence** 🚧 *(landed; live-DB verification pending)* | Supplier directory + create/detail/archive, contacts, locations, customer↔supplier relationship; supplier-portal-user invitation (`Membership.supplierId`); supplier portal (overview, questionnaire fill/submit, evidence refs); fixed sustainability questionnaire in `@trace/domain`; versioned **Supplier Passport** builder with per-field provenance; passport recompute on submit/accept; RLS on all supplier tables (`0004`); NordWerk seed extended with 20 suppliers + 3 completed passports. | An enterprise invites a supplier and collects sustainability information. **Status:** lint / typecheck / 43 unit tests / build green; supplier-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **3 — Evidence infrastructure** 🚧 *(landed; live-DB verification pending)* | `@trace/storage` package (`StorageService` + local-disk driver with HMAC-signed download URLs + S3 driver); document upload (multipart, MIME allowlist + magic-byte check, SHA-256, content-addressed dedupe, malware-scan hook); `Document`, `Evidence` (lifecycle state machine in `@trace/domain`, versioned via `supersedesId`), `Datapoint`, `datapoint_evidence`, `EvidenceVerification`; evidence transitions with permission gating (`evidence.verify` for verify/reject); evidence viewer + Evidence-DNA datapoint page; supplier evidence-ref → Evidence promotion; portal document upload; migrations `0005`/`0006` (+ RLS). Seed adds 2 verified evidence records + 3 linked datapoints. | Any sustainability datapoint can be linked to evidence. **Status:** lint / typecheck / 54 unit tests / build green; evidence-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **4 — Carbon engine** 🚧 *(landed; live-DB verification pending)* | `@trace/domain` carbon engine: unit registry + `convert` (decimal.js, exact), `computeEmission` (deterministic, stores every step, `recompute` reproduces bit-for-bit), `selectEmissionFactor` (explainable ranking), `summariseInventory`/`aggregateEmissions` (Scope 2 location vs market). DB: `emission_factor` (versioned, library + org), `activity_data` (+`activity_evidence`), immutable `calculation` (all inputs by value + reference + steps + factor-selection reasons), `emission` rollup projection; `datapoint.calculation_id`. Helpers `runCalculation` / `reproduceCalculation` / `recomputeCalculation` / `recomputeEmissions`. API: emission-factors, activity-data, calculations (run / lineage / reproduce / recompute / approve), emissions (summary / recompute). Web: Activity Data, Calculations, Scope 1/2/3, Emission Factors, and the **calculation lineage** page; Command Center shows real Scope 1/2/3 totals. Migrations `0007`/`0008` (+ RLS). Seed: 8 illustrative library factors + 1 supplier-specific, 6 activities, calculations, FY2025 rollup, Rheinstahl evidence→activity link. | A user can reproduce every emissions calculation exactly from stored inputs. **Status:** lint / typecheck / 79 unit tests / build green; carbon-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **5 — AI document intelligence** 🚧 *(landed; live-DB verification pending)* | `@trace/ai` package: `AIProvider` interface + **Claude adapter** (forced tool-use, Zod-validated output, token/cost accounting) + a deterministic **dev stub** (heuristic extraction, clearly labelled — used when no key). Deterministic pre-processing (`parseDocument`: text/csv native, PDF via pdf-parse). Capabilities `classifyDocument` + `extractDatapoints` with versioned prompts (`prompts/*.md` mirrors). DB: `ai_job` (every call recorded before use), `document_extraction` (parsed artifact + pipeline state), `candidate_datapoint` (AI output — never trusted). `@trace/db` orchestrator `runExtractionPipeline` (parse → classify → extract → candidates) + `promoteCandidate` (→ Datapoint + evidence from the document, human-gated) / `rejectCandidate`. API: `POST /documents/:id/process`, `GET /documents/:id/extraction`, `candidate-datapoints` (queue / promote / reject), `ai-jobs` (log). Worker: `document-processing` BullMQ queue. Web: Review Queue + candidate review screen (source span highlighted in parsed context), AI Log. Migrations `0009`/`0010` (+ RLS). Seed: a demo report processed via the stub → candidates, 2 promoted. | A supplier PDF becomes structured, reviewable data through a human-gated AI workflow. **Status:** lint / typecheck / 89 unit tests / build green; ai-extraction-flow + RLS integration tests run in CI; not yet run against a live DB or a live Claude key locally. |
| **6 — Trust engine** 🚧 *(landed; live-DB verification pending)* | `@trace/domain/trust`: `scoreDatapoint` (documented additive **TRACE Trust Score**, `trust-model@1.0.0` — primary data / evidence / verified / factor quality / unit-methodology / period freshness / completeness, 0–100 with a stored per-dimension breakdown), `evaluateDatapointQuality` (`quality-rules@1.0.0`, 12 issue kinds: missing value/unit/evidence, unverified/expired evidence, stale data, unit inconsistency, impossible value, duplicate, conflicting supplier report, outdated factor, low methodology tier), `detectAnomalies` (`anomaly-detector@1.0.0` — modified z-score over history + peers, period-over-period step). DB: immutable version-chained `trust_score`, idempotent `data_quality_issue` (dedupe key, auto-resolve, sticky dismiss) + `anomaly`, `quality_scan` run record; migrations `0011`/`0012` (+ RLS). Orchestrators `scoreDatapointTrust` / `runQualityScan` / `updateIssueStatus` / `updateAnomalyStatus`, all audit-logged. API: `/trust/scores` (+ `/:datapointId` + `/recompute`), `/data-quality/{summary,scans,scan,issues,anomalies}`; perms `trust.read` / `trust.run` / `quality.manage`; the datapoint Evidence-DNA response carries its score, issues and anomalies. Web: **Data → Data Quality** dashboard + scan history + run, issue and anomaly triage queues, and the Trust Score breakdown on the datapoint page. Seed: FY2025 scan over the NordWerk tenant. | A user can see, and explain from its breakdown, how trustworthy every major datapoint is. **Status:** lint / typecheck / 124 unit tests / build green; trust-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **7 — Compliance** 🚧 *(landed; live-DB verification pending)* | **New package `@trace/compliance`** (pure, `@trace/shared` only): a **versioned rule store** (frozen typed data, `esrs@2026.1` — ESRS E1-1 transition plan, E1-4 targets, E1-5 energy & mix, E1-6 gross Scope 1/2/3) + the generic **mapping engine** `evaluateRuleStore` (objective status ladder `not_started → data_available → evidence_available → mapping_complete`, `review_required` override; `aggregation` reconciles additive candidates; `mapping_complete` needs a human confirmation + Trust Score ≥ threshold) + `rollUpDisclosureStatus` / `readinessPct`. DB: global `regulation` / `requirement` / `disclosure` / `required_datapoint` / `evidence_requirement` (repository-scoped, like the factor library) + tenant `compliance_control` / `compliance_mapping` / `disclosure_status` / `compliance_run`; migrations `0013`/`0014` (+ RLS on the tenant tables only). Orchestrators `loadRuleStore` (idempotent), `runComplianceEvaluation` (idempotent upsert, preserves `confirmed`), `confirmMapping`, `upsertControl` — all audit-logged. `db → shared, domain, ai, compliance`. API: `/compliance/{overview,rule-stores,evaluate,gaps,runs,disclosures/:id,mappings/:id/confirm,controls}` (`compliance.read` / `compliance.manage`). Web: Compliance → Requirements (tree + readiness + load/evaluate), Disclosures, a disclosure **"why"** page (required datapoint → matched datapoint → calculation → evidence + gap reasons + confirm), Gaps — every screen carries a "not legal advice / never compliant" disclaimer. Seed: loads `esrs@2026.1`, evaluates FY2025, confirms the Scope 3 purchased-goods mapping, records two E1-6 controls. ADR-007. | A user traces requirement → datapoint → calculation → evidence, and every status is objective (never "compliant"). **Status:** lint / typecheck / 148 unit tests / build green; compliance-flow + RLS integration tests run in CI; not yet run against a live DB locally. |

At the end of Phase 7 the vertical-slice demo runs on the **NordWerk Manufacturing AG**
seed.

## Mid term — auditor's tool, executive's tool

| Phase | Scope |
| --- | --- |
| **8 — Audit workspace** 🚧 *(landed; live-DB verification pending)* | `@trace/domain/audit`: `assessReadiness` (`audit-readiness@1.0.0`) — a documented additive 0–100 score over evidence verified (25), calculations reproducible (20) / approved (15), data quality (15), Trust level (10), compliance mapping (10), audit-trail integrity (5), with a per-dimension breakdown **and** itemised issues (each pointing at its object). DB: `audit` engagement, `audit_finding` (idempotent `dedupeKey`, auto-resolve, sticky `accepted_risk`/`dismissed`; `manual` vs `simulation` source), `audit_simulation_run`, `audit_package`; migrations `0015`/`0016` (+ RLS). Orchestrators `runAuditSimulation` (composes `reproduceCalculation` + `verifyAuditChain` + data-quality + Trust + compliance; one audit entry), `createAudit`/`updateAudit`, `createFinding`/`updateFinding`, `generateAuditPackage` (canonical-JSON bundle → object storage, content-addressed by SHA-256: meta+disclaimer, organization, readiness, inventory, evidence+verifications, calculations+steps+live reproduce, datapoints+lineage+Trust, compliance mappings+gaps, open findings, audit-log chain check), `evidenceReviewList`, `evidenceChain`. `putBytes` injected so db doesn't import storage. API: `/audit/{readiness,simulate,simulations,audits,findings,evidence-review,evidence-chain/:id}` + `/audit/packages{,/:id,/:id/download}` (`audit.read`/`audit.manage`; `audit.manage` added to sustainability_manager). Web: Audit → Readiness, Findings (triage), Evidence Review + per-datapoint **evidence-chain** walk, Controls, Audit Package (generate + download). Seed: FY2025 engagement + simulation + 2 triaged findings + generated package. ADR-008. | An auditor reviews the evidence chain (disclosure → datapoint → calculation → activity → factor → evidence) and exports an audit package without engineering help. **Status:** lint / typecheck / 157 unit tests / build green; audit-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **9 — Command Center** 🚧 *(landed; live-DB verification pending)* | One read-only aggregate `@trace/db.commandCenterOverview` composes what Phases 3–8 already produce — GHG inventory + a multi-period trend (`inventorySummary` per period), data-provenance mix (measured / supplier-reported / calculated / estimated …, + primary-data share), Trust distribution (`qualitySummary`), audit readiness + open findings (latest `audit_simulation_run`), compliance progress (`disclosure_status` + latest `compliance_run` + top gaps), supplier coverage, and recent activity — into a single payload. No fabricated metrics: every number is read straight from a model row or an existing engine; no writes, no new domain package. API `GET /command-center/overview` (`organization.read`). Web: the Command Center page rebuilt on the one endpoint — band-coloured readiness / Trust / compliance / primary-data / coverage gauges, an inline SVG emissions-trend sparkline, provenance and Trust stack-bars, top compliance gaps and audit findings (all navigable). Seed adds an FY2024 comparative so the trend has two real points. | An executive view where every figure is real, sourced and one click from its detail. **Status:** lint / typecheck / 157 unit tests / build green; command-center-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **10 — Ask TRACE** 🚧 *(landed; live-DB verification pending)* | Natural-language questions answered by **retrieval over the tenant's own records** — the model never authors a query and never answers tenant facts from parametric knowledge (ADR-009). `@trace/ai` gains the `nl_analytics` capability: `classifyAskIntent` maps a question to one of 11 fixed intents (+ an optional period), `composeAskAnswer` writes an answer **grounded only in a set of numbered records** and returns which it cited; both are versioned prompts (`ask/intent@1`, `ask/answer@1`) and both work with the deterministic stub. `@trace/db.ask.ts` holds a catalog of **hand-written, tenant-scoped retrievals** (emissions summary / trend, top Scope 3 categories, top suppliers by emissions, missing evidence, estimated datapoints, outdated factors, low-Trust datapoints, compliance gaps, open findings, data-quality issues) and the `runAskQuery` orchestrator: both model calls are recorded as `ai_job` rows before use; if retrieval returns nothing (or the intent is `unsupported`) the answer says so and no answer-composition call is made. Model `ask_query` (question, intent, answer, `answered`, `citations` with UI hrefs, `aiJobIds`, cost); migrations `0017`/`0018` (+ RLS). API `/ask` (+ `/history`, `/:id`), permission `ask.use`. Web `/ask` — a question box with example prompts, the answer with clickable citation links + an intent / provider badge, and recent questions. Seed: four demo questions answered through the stub. | Every factual answer carries source/context links; an unlinked factual claim is a bug. **Status:** lint / typecheck / 160 unit tests / build green; ask-flow + RLS integration tests run in CI; not yet run against a live DB or a live Claude key locally. |
| **11 — Procurement intelligence** 🚧 *(landed; live-DB verification pending)* | `@trace/domain/procurement`: `compareSuppliers` (`procurement@1`, pure) — carbon **intensity** (tCO2e per €1,000 spend), emission/spend shares, an intensity ranking, an `attributionQuality` per row (`supplier_specific` / `spend_based` / …), flags, and **deterministic** reduction opportunities (bring an above-median supplier to the peer median — quantified; replace a spend-based estimate with primary data; onboard a passport-less tier-1 supplier). `projectScenario` — a pure what-if that re-runs `computeEmission` on a set of supplier lines with per-line changes (`activityMultiplier`, `factorValue`, `methodology`, `drop`) and reports baseline → projected → delta, exact via decimal.js. `@trace/db/procurement.ts`: `supplierCarbonComparison` (spend-based EEIO screen, refined where supplier-specific calculations exist), `scenarioLinesForSuppliers` (lines from each supplier's largest attributed calculation), `runProcurementScenario` (persists an **immutable** `procurement_scenario`, audit-logged). Model `procurement_scenario`; migrations `0019`/`0020` (+ RLS). API `/procurement/{suppliers,scenarios/lines,scenarios,scenarios/:id}` (`supplier.read`; saving needs the new `procurement.scenario` perm). Web: Supply Chain → **Carbon Map** (ranked table + intensity bars + reduction opportunities) and **Procurement Scenarios** (pick suppliers → default lines → reduce % / new factor / drop → project & save). Seed: spend-based Scope 3 attributed to every tier-1 supplier + a saved demo scenario. ADR-010. | A procurement manager compares suppliers by carbon and models a switch, on real calculation inputs. **Status:** lint / typecheck / 173 unit tests / build green; procurement-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **12 — Enterprise integrations** 🚧 *(landed; live-DB verification pending)* | Bulk activity data in, with per-row validation before anything is written (ADR-011). **New `@trace/domain/integrations`** (pure): `parseDelimited` (dependency-free RFC-4180-ish CSV/TSV — quoted fields, `""` escape, embedded delimiters/newlines, CRLF, BOM), an `IntegrationAdapter` contract (`targetFields` / `sampleHeaders` / `preview`, no I/O), `CsvActivityAdapter` (`csv_activity` — maps columns → the activity-data shape, validates every row: required-missing, non-numeric/negative quantity, bad enum, bad `YYYY-MM-DD`, unit not in the TRACE registry, non-UUID subject/supplier id), and a `registry`. `@trace/db/integrations.ts`: `previewImport` (pure, no transaction — every row with its `raw`, typed `mapped` or `null`, and `errors[]` + a valid/invalid summary) and `commitImport` (re-previews, writes **only** valid rows as `activity_data` stamped `source_ref = import:<run>:<line>`, `provenance` from the mapped column else `estimated`, per-row try/catch, one `integration.import_completed` audit entry); saved connectors `createIntegration` / `updateIntegration` / `listIntegrations` + `listIntegrationRuns` / `integrationRunById`. Models `integration` / `integration_run` (stores mapping, defaults, first 200 preview rows, per-row errors, `createdActivityIds`, file name + SHA-256, timings); migrations `0021`/`0022` (+ RLS `FORCE` on both). API `/integrations/{adapters,,:id,imports/preview,imports/commit,runs,runs/:id}` — reads need `activity.read`, configuring/importing needs the new `integration.manage` perm; 5 MB / text-only upload guard. Web: Settings → Integrations (connectors + run history) and an Import wizard (upload → auto-map → edit mapping/defaults → per-row preview → import the valid rows; nothing written until "Import"). Seed: a saved "Monthly facility energy — CSV" connector + a 3-row FY2025 facility-energy import. ADR-011. | An analyst brings a year of facility energy in from a spreadsheet in one pass, sees per-row validation first, and every imported row keeps its lineage. **Status:** lint / typecheck / 186 unit tests / build green; integrations-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **13a — Enterprise access** 🚧 *(landed; live-DB verification pending)* | The programmatic-access + fan-out + custom-role slice of Phase 13 (ADR-012); SSO / MFA / SCIM / export / retention / billing / usage limits are still open. **New `@trace/domain/access`** (pure): API-key format + `sha256` hashing + constant-time compare, coarse **scopes** (`read:all`, `activity:write`, …) that expand to a permission set and can **never** include an admin/self-propagating permission, a webhook **event catalog** + `webhookEventForAuditAction` map + HMAC `t=,v1=` signing + capped-exponential backoff, and `validateCustomRole`. `@trace/db/access.ts`: `createApiKey` / `revokeApiKey` / `listApiKeys` / `authenticateApiKey` (global lookup by `token_prefix`, throttled `last_used_at`); `create/update/delete/rollSecret/listWebhookEndpoints`; `list/retry/sendTest` deliveries; `dispatchDueWebhookDeliveries(prisma, { fetch })` — sign → POST → record → reschedule, `dead` after 6, endpoint auto-`disabled` after 15 straight failures. **`writeAuditLog` fans out**: a webhook-mapped action queues one `webhook_delivery` per subscribed active endpoint **in the same transaction**. Models `api_key` / `webhook_endpoint` / `webhook_delivery` + `role.is_system`; migrations `0023`/`0024` (RLS `FORCE` on `webhook_endpoint`; `api_key` + `webhook_delivery` repository-scoped like `session` / `audit_log`). API: `AuthGuard` gains an `x-api-key` / `Bearer` path (CSRF-exempt); `/api-keys` + `/webhooks` modules (`apikey.manage` / `webhook.manage`); `RolesController` gains custom-role `POST` / `PATCH` / `DELETE`; `/members/:userId/roles` sets a member's roles (last-admin-lockout guard). Worker: a 15s `dispatchDueWebhookDeliveries` interval. Web: Settings → **API Keys**, **Webhooks** (+ delivery inspector / retry), a **custom-role editor**, and an inline member role editor. Seed: a read-only demo key (printed once), a webhook endpoint that the later trust/compliance/audit steps really fan out to, and a `data_steward` custom role. ADR-012. | A platform team calls the API with a scoped key and receives signed webhook events for evidence, calculations, compliance and audit; an admin defines a custom role. **Status:** lint / typecheck / 210 unit tests / build green; access-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **13b — Enterprise identity & governance** 🚧 *(landed; live-DB verification pending)* | The account-security + data-governance slice of Phase 13 (ADR-013); SSO / SCIM / audit-log streaming / monitoring / billing / usage limits are still open. **`@trace/domain/access`** gains (pure): `totp.ts` — base32 + RFC-6238 HOTP/TOTP + a drift-tolerant `verifyTotp` + `otpauthUrl` + single-use recovery-code gen/hash; `export-bundle.ts` — the 24-section catalogue + `buildExportManifest` + 7-day TTL; `retention.ts` — `RETENTION_TARGETS` (operational/derived data only — never evidence, calculations, datapoints or the audit log), `validateRetentionPolicy`, `retentionCutoff`. `@trace/db`: `mfa.ts` (`beginMfaEnrollment` / `confirmMfaEnrollment` → one-time recovery codes / `verifyMfaChallenge` (TOTP or a consumed recovery code) / `disableMfa` / `resolveMfaRequirement`) and `governance.ts` (`runExport` — reads every tenant section, canonical-JSON, sha256, `putBytes` to `exports/<org>/<sha256>/export.json`, 7-day expiry; `upsertRetentionPolicy` / `runRetention` — `dry_run` counts, `apply` deletes, refused under legal hold). Models `user_mfa` (user-keyed, not RLS'd like `session`) / `export_job` / `retention_policy` / `retention_run` + `organization.require_mfa` / `legal_hold`; migrations `0025`/`0026` (RLS `FORCE` on the 3 tenant tables). API: `AuthGuard` MFA gate (`403 auth.mfa_required` on every route bar `@Public()` / a tiny `@MfaExempt()` allowlist) + `POST /auth/mfa`; `/me/mfa/{,setup,confirm,disable}`; `/exports/{,:id,:id/download}` (`data.export`, granted to auditor + finance); `/settings/security` + `/settings/retention/*` (`security.manage`). Worker: an hourly retention **dry-run** sweep over every active org (never deletes). Web: an `/auth/mfa` challenge page (the app layout redirects an unverified session there) + Settings → **Security** (self-enrolment, recovery codes, org `requireMfa` / `legalHold`) and **Data & Retention** (create/download exports; per-target policy editor; dry-run / apply; run history). Seed: a full-tenant export bundle + 3 retention policies with a recorded dry-run. ADR-013. | A user secures their account with an authenticator app, an admin exports a signed copy of every record and sets how long operational data is kept. **Status:** lint / typecheck / 227 unit tests / build green; governance-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **13c — Usage metering & quotas** 🚧 *(landed; live-DB verification pending)* | The metering slice of Phase 13 (ADR-014) — no payment integration; SSO / SCIM / audit-log *streaming* / monitoring are still open. **`@trace/domain/access`** gains (pure): `metering.ts` — `USAGE_METRICS` (api_request / ai_job / calculation_run / export_job / seats), `ENFORCED_METRICS`, `PLAN_TIERS` (free / growth / enterprise, per-metric monthly quota + `softWarnPct`), `billingPeriodKey` (`YYYY-MM` UTC), `evaluateUsage` (`ok`/`warn`/`over`), `wouldExceedQuota`, `crossedSoftWarn`; `audit-egress.ts` — `normalizeAuditFilter` (shape-guards every filter against injection), `toNdjson`, `AUDIT_EXPORT_MAX_ROWS = 20 000`. `@trace/db`: `metering.ts` — `loadPlans` (idempotent, into the `plan` catalogue), `ensureSubscription` (get-or-create + roll `currentPeriod`), `setPlan` (audit `billing.plan_changed`), `recordUsage` (period-counter upsert + a `usage_event` for non-request metrics + a `usage.threshold_reached` audit entry on the soft-warn crossing → fans out to webhooks), `recordApiRequest` (raw hot-path helper), `currentUsage`, `checkQuota`; `audit.ts` gains `queryAuditLog` (filtered + cursor) and `exportAuditLog` (NDJSON, oldest-first, capped). Models `plan` (global catalogue) / `subscription` / `usage_counter` (`@@unique[org,period,metric]`) / `usage_event`; migrations `0027`/`0028` (RLS `FORCE` on subscription / usage_counter / usage_event; `plan` global). `provisionOrganization` now loads plans + a default subscription; `usage_event` joins the retention targets. API: a global `UsageInterceptor` (fire-and-forget `api_request` + the `@Metered(metric)` increment) + a `QuotaGuard` (`429 quota.exceeded` for the enforced metrics); `@Metered('ai_job'|'calculation_run'|'export_job')` on the three expensive routes; `UsageModule` (`GET /usage`, `GET /usage/plans`, `PUT /usage/plan` — `usage.read` / `billing.manage`); `GET /audit-log` gains filters + a new `GET /audit-log/export` (NDJSON). Worker: a 6-hour subscription period roll-forward. Web: Settings → **Usage & Plan** (per-metric bars vs quota + plan switch) and, on the Activity Log, an action-prefix filter + NDJSON export. Seed: the demo org on the `growth` plan with a populated counter set. ADR-014. | An admin sees exactly what the workspace consumed this month against the plan, an over-quota tenant is stopped with a 429, and the full activity log exports as NDJSON. **Status:** lint / typecheck / 240 unit tests / build green; metering-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **13d — Audit-log streaming + monitoring** 🚧 *(landed; live-DB verification pending)* | The push-egress + observability slice of Phase 13 (ADR-015); SSO / SCIM and a real billing provider are the only open Phase-13 items. **`@trace/domain/access`** gains (pure): `audit-stream.ts` — `AuditStreamFilter` + `normalizeAuditStreamFilter` (regex-guards prefixes/resource types) + `matchesAuditStream`, `buildAuditStreamBatch`, `auditStreamNextAttemptAt` (0/15s/1m/5m/15m/1h/3h/6h), batch/attempt/auto-pause constants; `prometheus.ts` — `renderPrometheus` (0.0.4 text exposition format) + `PROMETHEUS_CONTENT_TYPE`; `health.ts` — `rollUpHealth`, `buildHealthReport`, `heartbeatStatus` (up<2m / degraded<10m / down). `@trace/db`: `audit-stream.ts` — CRUD (`createAuditStream` starts the cursor at the current log head — no back-fill; `rotateAuditStreamSecret`; `sendTestAuditStream`) + `dispatchOrgAuditStreams` (per active stream: retry an in-flight delivery, else read the next `audit_log` window after the cursor, filter, POST a signed batch [same `t=,v1=` HMAC as webhooks]; cursor advances on success even when nothing matched; a `dead` batch is skipped past; 20 straight failures auto-pause) + `writeHeartbeat` / `readHeartbeat`; `ops.ts` — `orgStats` (per-tenant snapshot) and `platformMetrics` (process-wide `MetricSample[]`). Models `audit_stream` / `audit_stream_delivery` / `component_heartbeat`; migrations `0029`/`0030` (RLS `FORCE` on `audit_stream` only; `audit_stream_delivery` repository-scoped like `webhook_delivery`; `component_heartbeat` global). API: `AuditStreamsModule` (`/settings/audit-streams` CRUD + `/:id/{rotate-secret,test}` + `/deliveries` — `audit_stream.manage`); `OpsModule` (`GET /ops/stats` — `ops.read`; `GET /metrics` — `@Public()`, `METRICS_TOKEN` bearer when set, `text/plain`); `GET /health/detailed` (`@Public()` — DB round-trip + worker heartbeat + storage driver, rolled up). `@trace/config` gains `METRICS_TOKEN`. Worker: a 20s audit-stream dispatch sweep + a 30s worker heartbeat. Web: Settings → **Audit Streams** (create with an action-prefix filter, one-time secret, test / pause / delete, delivery log) and **Ops & Health** (org stat cards + worker heartbeat). Seed: a demo audit stream (`compliance.` / `audit.` / `evidence.` / `security.`) + a primed heartbeat. ADR-015. | Every activity-log entry is pushed to the tenant's SIEM as a signed batch, and an operator scrapes `/metrics` and reads a rolled-up `/health/detailed`. **Status:** lint / typecheck / 254 unit tests / build green; audit-stream-flow + RLS integration tests run in CI; not yet run against a live DB locally. |
| **13e — OpenID Connect SSO** 🚧 *(landed; live-DB verification pending)* | Per-organization OIDC sign-in with just-in-time provisioning (ADR-016); SAML, SCIM and a real billing provider are the only open Phase-13 items. **New `@trace/domain/access/oidc.ts`** (pure): `generatePkce` / `pkceChallengeFor`, `randomUrlToken` (state / nonce), `buildAuthorizationUrl`, `verifyIdToken` (RS256 only — rejects `alg:none` / HS\*; signature via `crypto.createPublicKey({format:'jwk'})` + `crypto.verify`; then `iss` / `aud` / `exp` ±120s / `nbf` / `sub` / `nonce`), `mapClaimsToRoleKeys` (default + email-domain + IdP-group → role keys), `emailDomainAllowed`. 12 unit tests. `@trace/db/sso.ts`: `upsertIdentityProvider` (validates https endpoints + that every mapped role exists; `clientSecret` optional on update; audit), `getIdentityProvider` (never returns the secret) / `deleteIdentityProvider`, `beginSsoLogin(prisma, {orgSlug, redirectUri, redirectAfter?})` → `{authorizationUrl}` (persists a single-use `sso_login_request` with `state` / `nonce` / PKCE verifier, 10-min TTL), `completeSsoLogin(prisma, deps, {state, code, redirectUri})` → `{userId, organizationId, roleKeys, provisioned, redirectAfter}` (`deps.exchangeCode` + `deps.fetchJwks` injected; verifies the token, enforces `allowedEmailDomains`, upserts the platform `user` by email with `external_id = sub`, then in `withOrgContext` upserts `sso_link` + creates a `membership` with the mapped roles if none exists), `listSsoLinks`, `pruneSsoLoginRequests`. Models `identity_provider` (one per org, holds `client_secret`) / `sso_login_request` (not RLS'd, like `magic_link_token`) / `sso_link` + `user.external_id`; migrations `0031`/`0032` (RLS `FORCE` on `identity_provider` + `sso_link`). API: `SsoModule` — `GET /auth/sso/:slug/start` (302 to the IdP) and `GET /auth/sso/:slug/callback` (verify → JIT → session → 302 to the web; both `@Public()` + `@MfaExempt()`; errors → `/login?sso_error=`), `GET/PUT/DELETE /settings/sso` + `POST /settings/sso/discover` (`security.manage`); real token-exchange / JWKS fetch via global `fetch` with an 8s timeout. Worker: the housekeeping pass also prunes expired login requests. Web: `/login` gains a "Continue with SSO" box + surfaces `?sso_error=`; Settings → **Single Sign-On** (issuer + Discover, endpoints, client credentials, scopes, role mapping, allowed domains, enable; shows the redirect URI to register). Seed: a **disabled** demo OIDC provider for the demo org. ADR-016. | A member signs in through their identity provider and is provisioned into the workspace with roles from the claim mapping. **Status:** lint / typecheck / 266 unit tests / build green; sso-flow + RLS integration tests run in CI; not yet run against a live DB or a live IdP locally. |
| **13f — SAML 2.0 SSO** 🚧 *(landed; live-DB verification pending)* | Per-organization SAML 2.0 Web-Browser-SSO with just-in-time provisioning (ADR-017); SCIM 2.0 and a real billing provider are the only open Phase-13 items. **New `@trace/domain/access/saml.ts`** (pure, + `xml-crypto` / `@xmldom/xmldom` — both do pure computation, no I/O): `generateSamlId` / `generateRelayState`, `buildAuthnRequestXml` + `buildRedirectBindingUrl` (SP-initiated HTTP-Redirect binding — `SAMLRequest = base64(DEFLATE(xml))`, no request signing), `buildSpMetadataXml`, `normalizeCertificatePem`, `verifySamlResponse(xml, {certificatesPem, spEntityId, acsUrl, expectedInResponseTo, idpEntityId, wantAssertionsSigned?, now?})` → `{ok, nameId, nameIdFormat, sessionIndex, attributes} | {ok:false, reason}` — XML-DSig via `xml-crypto` (identity read only from `getSignedReferences()` — defeats signature-wrapping), **plus**: exactly one `<Assertion>`, every `<ds:Signature>` must sit directly on the Response or that Assertion, RSA-SHA-256/384/512 only (SHA-1 rejected), `Issuer` / `Status` / bearer `SubjectConfirmationData` (`Recipient` == ACS, `InResponseTo` == our request ID, `NotOnOrAfter` ±120s) / `Conditions` window / `AudienceRestriction` contains our SP entityID / response `Destination`. `extractSamlIdentity` (configured attribute → well-known URI → NameID) + `mapSamlToRoleKeys` (reuses the 13e `OidcRoleMapping` engine). 14 unit tests (real signing + verification with a throwaway RSA key, wrapping / tamper / wrong-key rejection). `@trace/db/saml.ts`: `upsertSamlProvider` (SSO URL https; each cert parses as X.509 / public key; every mapped role exists; audit `saml.provider_configured` / `_updated`), `getSamlProvider` (+ `linkedMembers`) / `deleteSamlProvider`, `beginSamlLogin(prisma, {orgSlug, acsUrl, spEntityId, redirectAfter?})` → `{redirectUrl}` (persists a single-use `saml_login_request` with `saml_request_id` / `relay_state`, 10-min TTL), `completeSamlLogin(prisma, {samlResponse, relayState, acsUrl, spEntityId})` → `{userId, organizationId, roleKeys, provisioned, redirectAfter}` (no injected deps — the POST binding delivers the assertion straight to the ACS; verifies, enforces `allowedEmailDomains`, upserts the platform `user` by email with `external_id = NameID`, then in `withOrgContext` upserts `saml_link` + creates a `membership` with the mapped roles if none exists), `listSamlLinks`, `pruneSamlLoginRequests`. Models `saml_provider` (one per org; only the public signing certs are stored) / `saml_login_request` (not RLS'd, like `sso_login_request`) / `saml_link`; migrations `0033`/`0034` (RLS `FORCE` on `saml_provider` + `saml_link`). API: `SsoModule` gains `SamlAuthController` — `GET /auth/saml/:slug/start` (302 to the IdP), `POST /auth/saml/:slug/acs` (verify → JIT → session → 302 to the web), `GET /auth/saml/:slug/metadata` (SP metadata XML); all `@Public()` + `@MfaExempt()`, the ACS CSRF-exempt via `@Public()`; errors → `/login?sso_error=` — and `SamlConfigController` (`GET/PUT/DELETE /settings/saml` — `security.manage`). Worker: the housekeeping pass also prunes expired SAML login requests. Web: `/login` gains a protocol selector (OpenID Connect / SAML 2.0) on the SSO box; Settings → **SAML SSO** (IdP entity ID + SSO URL + signing certificates, attribute mapping, role mapping, allowed domains, enable; shows the ACS URL / SP entity ID / metadata URL to register). Seed: a **disabled** demo SAML provider for the demo org. ADR-017. | A member signs in through their SAML identity provider and is provisioned into the workspace with roles from the attribute mapping. **Status:** lint / typecheck / 280 unit tests / build green; saml-flow + RLS integration tests run in CI; not yet run against a live DB or a live IdP locally. |
| **13g — SCIM 2.0 provisioning** 🚧 *(landed; live-DB verification pending)* | The provisioning slice of Phase 13 (ADR-018); a real billing-provider integration is the only open Phase-13 item. TRACE is the SCIM **service provider** — an IdP pushes user / group lifecycle over a bearer-token REST API. **New `@trace/domain/access/scim.ts`** (pure): token gen + constant-time compare, `parseScimUser` / `parseScimGroup`, `normalizeScimPatch` + `applyScimUserPatch` / `applyScimGroupPatch` (`add` / `remove` / `replace`; `name.givenName`, the `members[value eq "id"]` selector, no-path object merge), `scimUserResource` / `scimGroupResource` / `scimListResponse` / `scimError`, `parseScimFilter` (`attr eq "value"` only), `scimPaginationParams` (clamp `count` ≤ 200), `resolveScimRoleKeys` + `scimManagedRoleKeys`, and the ServiceProviderConfig / ResourceTypes / Schemas docs. 14 unit tests. `@trace/db/scim.ts`: `upsertScimConfig` / `getScimConfig` / `rotateScimToken` (token shown once, sha256 stored) / `deleteScimConfig`, `authenticateScim(prisma, {orgSlug, bearerToken})`, `scim{List,Get,Create,Replace,Patch,Delete}User` + `…Group`, `scimAdminOverview`, and an internal `reconcileMemberRoles` — a SCIM user is the projection of a `membership` (`active` ⇄ suspended), a SCIM group grants roles through `scim_config.group_role_mapping`, and the reconciler only ever adds / removes roles in the **managed set** (`defaultRoles` ∪ every mapped role) so hand-assigned roles are untouched. Every mutation is audit-logged. Models `scim_config` / `scim_user` / `scim_group` / `scim_group_member`; migrations `0035`/`0036` (RLS `FORCE` on the first three; the join follows `membership_role` and is not RLS'd). API: `ScimModule` — `ScimUsersController` / `ScimGroupsController` / `ScimDiscoveryController` under `/scim/v2/:orgSlug` (`@Public()` + a `ScimAuthGuard` bearer check + a `ScimExceptionFilter` rendering RFC 7644 `Error` objects + an `application/scim+json` interceptor), and `ScimConfigController` (`GET` / `PUT` / `POST /token` / `DELETE /settings/scim` — `security.manage`). `AuthGuard`'s API-key path was tightened to engage only for `trk_` tokens (so a `scim_…` bearer falls through); `main.ts` parses `application/scim+json` as JSON. Web: Settings → **SCIM Provisioning** — base URL + token (issue / rotate, shown once), enable toggle, default-role chips + group → roles JSON, and a read-only list of provisioned users (with resolved roles) and groups. Seed: a **disabled** demo SCIM connection with a token issued. ADR-018. | An IdP creates, updates and deprovisions members and groups over SCIM, and role assignment follows the group mapping. **Status:** lint / typecheck / 294 unit tests / build green; scim-flow + RLS integration tests run in CI; not yet run against a live DB or a live IdP locally. |

## Long term — become infrastructure

| Phase | Scope |
| --- | --- |
| **13h — Billing provider** | The last of Phase 13: a real billing-provider integration — a provider adapter (checkout session + subscription lifecycle) and signed webhook ingestion wired to the Phase-13c plan / quota engine. Needs a payment provider for honest end-to-end testing. |
| **14 — Carbon Twin / network** | Supply-chain graph, carbon hotspots, supplier network, product carbon footprints, scenario engine, network benchmarking. Introduce a graph database only if traversal needs justify it. |

## Per-phase workflow (Principle 39)

1. Understand the domain requirement.
2. Inspect the existing repository; never overwrite work without understanding it.
3. Identify architectural impact.
4. Create / update documentation + ADR if a decision is consequential.
5. Implement domain logic (`packages/domain` — pure, tested).
6. Implement database changes (`packages/db` — schema + migration + RLS).
7. Implement APIs (`apps/api` — thin controllers, Zod DTOs, guards, OpenAPI).
8. Implement frontend (`apps/web` + `packages/ui`).
9. Implement tests (unit / integration / e2e / security as applicable).
10. Lint. 11. Typecheck. 12. Unit tests. 13. Integration tests.
14. Security review (per `docs/security.md`).
15. UX review (per the Principle 42 checklist).
16. Update documentation.
17. Summarise what changed.

Commits are scoped and conventional:
`feat(evidence): implement evidence lifecycle`,
`feat(carbon): add deterministic calculation engine`, etc. No enormous unreviewable commits.

## Quality bar (Principle 40) — never merge with

TypeScript errors · failing tests · broken imports · unused code · placeholder production
logic · fabricated metrics · hard-coded user-specific data · duplicated business logic ·
security shortcuts · unvalidated API input · missing tenant checks · undocumented
architectural decisions.

## Definition of "demo-ready"

Running `pnpm db:seed` produces a live-looking **NordWerk Manufacturing AG** tenant: 20
suppliers, 5 facilities, 15 materials, 10 products, activity records, calculations,
emissions, evidence documents, compliance gaps, audit findings — all explicitly flagged as
seed/demo data.
